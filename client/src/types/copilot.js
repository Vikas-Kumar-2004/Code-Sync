/**
 * @typedef {Object} ICopilotContext
 * @property {Function} setInput - Function to set input string
 * @property {string} output - Generated output string
 * @property {boolean} isRunning - Flag indicating if code generation is running
 * @property {Function} generateCode - Function to generate code
 */

// Export empty object since we can't export types in JS
// This is now just documentation via JSDoc
export {}