/**
 * @typedef {Object} FileTabContext
 * @property {import("./file").FileSystemItem|null} activeFile - Currently active file or null
 * @property {Function} setActiveFile - Function to set the active file
 * @property {Function} changeActiveFile - Function to change active file by ID
 */

// Export empty object since we can't export types in JS
// This is now just documentation via JSDoc
export {}